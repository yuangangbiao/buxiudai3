# 源码问题 TODO — 测试体检中发现（2026-06-23）

> **来源**: 测试体检报告悲观审计  
> **原则**: 本次仅标记，不修改源码；另行修复  
> **优先级说明**: 🔴 高 = 安全/合规风险；🟠 中 = 维护性风险；🟡 低 = 代码规范

---

## 🔴 高优先级（安全/合规）

### SRC-001: 测试文件硬编码明文密码

| 字段 | 值 |
|------|-----|
| **文件** | `tests/test_business_correctness.py` |
| **位置** | 第 19 行 |
| **问题** | `password='88888888'` 明文硬编码 |
| **类型** | 凭证泄露（测试文件，但可能含真实密码片段） |
| **修复方案** | 改为 `os.environ.get('TEST_DB_PASSWORD')` 或读取 `.env` |
| **是否已标记** | ✅ 已加 `@pytest.mark.integration` |

### SRC-002: 微信云端地址硬编码

| 字段 | 值 |
|------|-----|
| **文件** | `mobile_api_ai/tests/integration/test_send_wechat.py` |
| **位置** | 第 10 行 |
| **问题** | `WECHAT_CLOUD_HOST = 'http://124.223.57.82:5006'` 硬编码 |
| **类型** | 配置硬编码（云端地址不应出现在代码中） |
| **修复方案** | 改为 `os.environ.get('WECHAT_CLOUD_HOST')` |
| **是否已标记** | ✅ 已加 `@pytest.mark.manual` |

---

## 🟠 中优先级（维护性）

### SRC-003: 45+ 处 DB 连接分散在 15+ 个 scripts/ 文件

| 字段 | 值 |
|------|-----|
| **问题** | 15+ 个 scripts/ 文件直接连真实 DB，`pymysql.connect` / `MySQLStorage` / `get_connection` 分散 |
| **影响** | 测试污染数据、连接池耗尽 |
| **修复方案** | 统一迁移到 `tests/integration/` 目录，加 `@pytest.mark.integration` |
| **已处理** | ✅ 已给 15 个文件加 marker（但未实际重构代码） |

### SRC-004: `core/database.py` 源文件已删除，孤儿备份残留

| 字段 | 值 |
|------|-----|
| **文件** | `mobile_api_ai/core/database.py`（源已删） |
| **残留** | `mobile_api_ai/core/database.py,cover`（孤儿备份） |
| **修复方案** | 确认无引用后删除 `,cover` 备份 |
| **操作** | ⚠️ **待用户确认**（`core/database.py` 可能是重构前的旧文件） |

---

## 🟡 低优先级（代码规范）

### SRC-005: 281 组跨文件同名测试函数

| 字段 | 值 |
|------|-----|
| **问题** | `test_get_all` 出现 134 次，`test_create` 出现 128 次 |
| **影响** | pytest `-k` 关键字筛选时同时命中多个模块，难以精确筛选 |
| **修复方案** | 加模块前缀：`test_<module>_<action>` |
| **优先级** | 低（pytest 不冲突，只是命名风格问题） |

### SRC-006: 39 组近似命名多版本测试

| 字段 | 值 |
|------|-----|
| **问题** | 同一模块有 `test_xxx.py` + `test_xxx_complete.py` + `test_xxx_depth.py` |
| **影响** | 重复测试、维护成本高 |
| **修复方案** | 保留 `_complete` 主版本，删除 `_depth`/`_gaps` |
| **优先级** | 低（不影响功能） |

### SRC-007: 24 个巨型测试文件（>500 行，最高 1344 行）

| 字段 | 值 |
|------|-----|
| **问题** | 单文件过长，pytest 慢，git 冲突多 |
| **修复方案** | 按业务流拆分为多个文件（每个 < 500 行） |
| **优先级** | 低（不影响功能） |

---

## 📊 源码问题汇总

| 编号 | 优先级 | 问题 | 影响范围 | 状态 |
|------|--------|------|---------|------|
| SRC-001 | 🔴 高 | `test_business_correctness.py` 明文密码 | 1 文件 | 待修复 |
| SRC-002 | 🔴 高 | `test_send_wechat.py` 硬编码云端地址 | 1 文件 | 待修复 |
| SRC-003 | 🟠 中 | 45+ 处 DB 连接分散 | 15+ 文件 | 已标记，待重构 |
| SRC-004 | 🟠 中 | `database.py,cover` 孤儿备份 | 1 文件 | 待确认 |
| SRC-005 | 🟡 低 | 281 组同名函数 | 281 组 | 可选 |
| SRC-006 | 🟡 低 | 39 组近似命名 | 39 组 | 可选 |
| SRC-007 | 🟡 低 | 24 个巨型测试 | 24 文件 | 可选 |

---

## 操作指引

```bash
# SRC-001 修复（明文密码）
# 文件: tests/test_business_correctness.py
# 改: password='88888888'
# 为: password=os.environ.get('TEST_DB_PASSWORD', '')

# SRC-002 修复（硬编码云端地址）
# 文件: mobile_api_ai/tests/integration/test_send_wechat.py
# 改: CLOUD_HOST = 'http://124.223.57.82:5006'
# 为: CLOUD_HOST = os.environ.get('WECHAT_CLOUD_HOST', 'http://124.223.57.82:5006')

# SRC-004 修复（孤儿备份）
# 文件: mobile_api_ai/core/database.py,cover
# 确认 core/database.py 确实已重构/删除后:
# del mobile_api_ai/core/database.py,cover
```