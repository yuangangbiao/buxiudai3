# TASK v3.7.0 - 业务代码 P0 + L1 冒烟测试

> **版本**: v3.7.0
> **阶段**: 6A 阶段 3 (Atomize)
> **日期**: 2026-06-25

---

## 任务依赖图

```
T1 (P0-2: DLQ retry worker)
   ↓
T2 (Q-B6: 删除废弃文件)
   ↓
T3 (Q-B7: 修复裸异常日志 5+ 处)
   ↓
T4 (L1 冒烟测试 5 个)
   ↓
T5 (ACCEPTANCE 验收)
```

并行任务:
- T2、T3 可并行
- T4 必须 T1/T2/T3 完成后

---

## T1: P0-2 DLQ retry worker

### 输入契约

| 项 | 内容 |
|----|------|
| 前置依赖 | 无（独立任务）|
| 输入数据 | MySQL `dlq` 表 schema |
| 环境依赖 | 调度中心 5003 服务、MySQL container_center 库 |

### 输出契约

| 项 | 内容 |
|----|------|
| 输出数据 | `mobile_api_ai/dispatch_center/_dlq_retry.py`（新文件） |
| 交付物 | 函数 `start_dlq_retry_worker` + 单元测试 + 文档 |
| 验收标准 | 启动调度中心时自动调用，扫描 dlq 表+重试逻辑生效 |

### 实现约束

| 项 | 约束 |
|----|------|
| 技术栈 | Python 3.14 + threading + pymysql |
| 接口规范 | R-001 不直连其他服务 DB（仅访问自己的 container_center）|
| 质量要求 | 代码风格遵循项目 v1.1 规则 |

### 依赖关系

- 后置任务: T5
- 并行任务: T2, T3

### 工作量: 4h

---

## T2: Q-B6 删除废弃文件

### 输入契约

| 项 | 内容 |
|----|------|
| 前置依赖 | 无 |
| 输入数据 | `desktop_container_integration.py` 内容 + 全文引用 |
| 环境依赖 | 无 |

### 输出契约

| 项 | 内容 |
|----|------|
| 输出数据 | 删除文件 + 清理引用 |
| 交付物 | 引用列表 + 清理结果 |
| 验收标准 | `grep -r desktop_container_integration` 0 匹配 |

### 实现约束

- 必须先搜索所有引用
- 删除前 git status 显示该文件
- 删除后 pytest 验证

### 工作量: 5min

---

## T3: Q-B7 修复裸异常日志

### 输入契约

| 项 | 内容 |
|----|------|
| 前置依赖 | 无 |
| 输入数据 | `_core.py` 中所有 `logger.error(f"...{e}")` 位置 |
| 环境依赖 | 无 |

### 输出契约

| 项 | 内容 |
|----|------|
| 输出数据 | 5+ 处修改 |
| 交付物 | 修改清单 |
| 验收标准 | grep 确认 + pytest 通过 |

### 实现约束

- 优先级: 报工/质检同步路径 + 写入路径
- 修改模式: `logger.error(f"...{e}")` → `logger.exception("...")`
- 不改业务逻辑

### 工作量: 1h

---

## T4: L1 冒烟测试 5 个

### 输入契约

| 项 | 内容 |
|----|------|
| 前置依赖 | T1/T2/T3 完成 |
| 输入数据 | 5 个核心业务流程 |
| 环境依赖 | tests/core/* fixtures |

### 输出契约

| 项 | 内容 |
|----|------|
| 输出数据 | `tests/L1_smoke/test_*.py` 5 文件 |
| 交付物 | 5 个测试用例 |
| 验收标准 | pytest 5min 内跑完，0 错误 |

### 实现约束

- 全 mock，离线运行
- 每个测试 ≤30s
- 使用 tests/fixtures/ 提供的数据

### 任务清单

| 文件 | 测试场景 |
|------|----------|
| test_login.py | 5 角色登录验证 |
| test_order_create.py | 订单创建+状态流转 |
| test_process_publish.py | 工序发布+任务接收 |
| test_quality_check.py | 质检记录+合格判定 |
| test_shipment.py | 发货+签收流程 |

### 工作量: 1天

---

## T5: ACCEPTANCE 验收

### 输入契约

| 项 | 内容 |
|----|------|
| 前置依赖 | T1-T4 全部完成 |
| 输入数据 | 所有任务交付物 |
| 环境依赖 | 完整测试栈 |

### 输出契约

| 项 | 内容 |
|----|------|
| 输出数据 | `docs/v3.7.0/ACCEPTANCE_v3.7.0.md` |
| 交付物 | 验收报告 + 业务影响 |
| 验收标准 | 所有 DoD 满足 |

### 工作量: 30min

---

**总工作量**: 1.5 天（4h + 5min + 1h + 1day + 30min）

---

**下一阶段**: [ACCEPTANCE_v3.7.0.md](ACCEPTANCE_v3.7.0.md)
