# 业务影响报告 - P0 修复联合工作组

> 报告时间: 2026-06-23 18:30
> 执行人: 小曦 (产品经理)
> 范围: 4 项 P0 修复（#2 #4 #5 #6 #7）

---

## 1. 用户场景对比（改善前 → 改善后）

| # | 用户角色 | 改善前（痛点） | 改善后（价值） |
|---|---------|---------------|---------------|
| 1 | 桌面 Web 端用户（排产/物料/质检/发货） | 登录后调 5003 业务接口**全部 401**, 操作员/订单/排产数据看不到, 客户端 401 闪退 (#4) | session 自动带正确 token, 业务接口 200, 看到完整数据 |
| 2 | 移动报工工人 (5008) | 报工时 `get_packages(data_type='process_report')` 返 `[]`, 报工历史 100% 丢失 (#2) | 报工历史正常显示 20+ 行, 报工数据可追溯 |
| 3 | 未登录攻击者 | 5001 桌面端 40 个写操作（创建订单/改模板/上传附件）**零鉴权**, 可直接 POST 篡改数据 (#5+#6) | 全部 46 个写路由未登录 401, CSRF 缺失 403, role 默认 'viewer' 无写权 |
| 4 | 调度中心 (5003) | 5001 转发来的 `secrets.token_hex(32)` 被 5003 鉴权协议拒 (#4) | 收到符合 5003 协议的 `base64(uid:uname)`, 鉴权一致通过 |
| 5 | 运维/客户演示 | `/api/process/admin-list` 因 `po.customer_name` 字段不存在报 500, 演示翻车 (#7) | 改用 `o.customer_name` + LEFT JOIN orders o, 端到端 200, 演示稳定 |

---

## 2. 业务能力新增

| 业务流 | 新增/优化功能 | 影响范围 | 关联 P0 |
|--------|--------------|---------|---------|
| **生产** | 5001 → 5003 跨服务链路恢复, 报工历史可查询 | 排产/工序报工/外协全链 | #4 #2 |
| **生产** | 报工并发锁 (FOR UPDATE) 防止数据覆盖 (前序 P0-3) | process_records / order_materials | (前序) |
| **质检** | 未登录无法创建质检记录 (需 viewer 以上) | /api/quality/* 写路由 | #5 #6 |
| **物料** | 未登录无法创建/导入物料, 需 CSRF token | /api/material/* 写路由 | #5 #6 |
| **外协** | get_packages 待补 6 个 key, P1 完善 | 6 个 type 分支 | #2 (P1) |
| **报修** | get_packages 待补 6 个 key, P1 完善 | repair_records | #2 (P1) |
| **监控** | 8008 sync_bridge 启动, 同步链路恢复 (无监控埋点) | 5001 ↔ 5008 Δ 同步 | (启动) |
| **安全** | 5001 role 默认 'worker'→'viewer', 阻断权限提升 | 全部写路由默认角色 | #5 |
| **安全** | 5003 mobile_login 真实查 role 列, 替代硬编码 'worker' | 5003 mobile_login | #6 |
| **鉴权** | 5001 ↔ 5003 token 协议统一 (base64 uid:uname) | ~20 个代理端点 | #4 |

---

## 3. 不变更部分（防回归保护清单）

| # | 模块/功能 | 保护措施 | 验证方式 |
|---|----------|---------|---------|
| 1 | `_call_dispatch` 函数本体 | 零修改, 仅依赖 `_get_token()` 返回正确值 | pytest 全过 + 端到端 200 |
| 2 | 前端 login.html (base64 编码) | 零修改, 算法与修复后 5001 一致 | git diff 验证 (无变更) |
| 3 | 5003 鉴权协议 `base64(uid:uname)` | 零修改, 5001 改为对齐 | 5003 端代码 git diff 验证 |
| 4 | CSRF token (secrets.token_hex) | 保留, 5001 自己的, 不影响 5003 | pytest 验证 session['csrf_token'] 仍存在 |
| 5 | 数据库表结构 (process_sub_steps / quality_records / material_records) | 零修改 | pytest 全过 |
| 6 | /api/login 不被加 @require_auth (避免登录死锁) | pytest test_p0_4_login_route_does_not_require_auth PASSED | E2E 登录返 200 |
| 7 | process_status_transitions / order_status_transitions 状态机 (前序 P0-4) | 零修改, 继续生效 | 非法状态变更返 400 |
| 8 | 连接池配置 (pool_size=30, 前序 P0-2.1) | 零修改 | 服务启动日志 `[DB] max=30` |

---

## 4. 一句话总结

> **本次 P0 修复联合工作组让 5001 桌面 Web 端从"登录后 5003 业务接口 100% 401 失败 + 40 个写操作零鉴权可被未登录篡改"变为"5001↔5003 token 协议统一, 46/46 写路由 CSRF+role 鉴权闭环, 报工历史 0→20 行可查, /api/process/admin-list 端到端稳定"**。
