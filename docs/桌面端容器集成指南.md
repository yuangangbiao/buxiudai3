# 桌面端容器集成指南

## 📋 概述

本指南介绍如何在桌面端跟单系统3.0中集成容器池功能，实现数据的完整流向：

```
桌面端（创建订单/工序）
    ↓
容器池（收集任务）
    ↓
网页端/手机端（扫码确认/报工）
```

---

## 📦 文件清单

| 文件 | 说明 |
|------|------|
| `desktop_container_integration.py` | 桌面端容器集成模块（新） |
| `process_view_integration_example.py` | 工序视图集成示例 |
| `mobile_api_ai/desktop_container_integration.py` | 旧API调用方式（已废弃） |

---

## 🚀 快速开始

### 1️⃣ 测试集成模块

先运行集成模块的演示，确保一切正常：

```bash
cd /path/to/project
python desktop_container_integration.py
```

预期输出：
```
============================================================
桌面端容器池集成演示
============================================================
[容器集成] 初始化成功

[1] 发布报工任务...
  订单: ORD202604001
  工单: WO202604001
  工序: 编织
  数量: 100
  ✅ 报工任务发布成功: xxx

[2] 发布质检任务...
  订单: ORD202604002
  质检类型: 终检
  ✅ 质检任务发布成功: xxx

[3] 容器池状态
...
```

### 2️⃣ 启动网页端服务（同时测试扫码获取数据）

```bash
cd mobile_api_ai
python wechat_work_bot_v2.py
```

在另一个终端运行测试脚本：

```bash
cd mobile_api_ai
python quick_test.py
```

---

## 💻 在工序视图中集成

### Step 1: 添加导入

在 `views/process_view.py` 的顶部添加：

```python
# 在现有import之后添加：
try:
    from desktop_container_integration import get_integration
    CONTAINER_AVAILABLE = True
except ImportError as e:
    print(f'[容器集成] 模块不可用: {e}')
    CONTAINER_AVAILABLE = False
```

### Step 2: 初始化集成实例

在 `ProcessView.__init__` 中：

```python
    def __init__(self, parent):
        super().__init__(parent, bg=COLORS["bg_main"])
        self.current_order_id = None
        self.current_prod_id = None
        
        # 初始化容器集成
        self.container_integration = None
        if CONTAINER_AVAILABLE:
            try:
                self.container_integration = get_integration()
                print('[工序视图] 容器集成已就绪')
            except Exception as e:
                print(f'[工序视图] 容器集成初始化失败: {e}')
        
        self.init_ui()
        self.load_orders()
```

### Step 3: 添加任务发布方法

在 `ProcessView` 类中添加：

```python
    def _publish_task_to_container(self, process_data: dict) -> Optional[str]:
        """
        发布工序任务到容器池
        
        Args:
            process_data: 工序数据字典，包含：
                - order_no: 工单号
                - process_name: 工序名称
                - planned_qty: 计划数量
                - operator_id: 操作员ID
                - operator_name: 操作员名称
                - order_no: 订单号
                - customer_name: 客户名称
                - product_type: 产品类型
                - quantity: 订单数量
                - unit: 单位
        
        Returns:
            任务ID，失败返回None
        """
        if not self.container_integration or not self.container_integration.is_available:
            print('[工序视图] 容器集成不可用，跳过发布')
            return None
        
        try:
            task_id = self.container_integration.publish_report_task(
                order_no=process_data.get('order_no', ''),
                order_no=process_data.get('order_no', ''),
                process_name=process_data.get('process_name', ''),
                customer_name=process_data.get('customer_name', ''),
                product_type=process_data.get('product_type', ''),
                quantity=process_data.get('quantity', 0),
                unit=process_data.get('unit', ''),
                planned_qty=process_data.get('planned_qty', 0),
                process_status='待开始',
                operator_id=process_data.get('operator_id', 'OP001'),
                operator_name=process_data.get('operator_name', ''),
                priority='normal'
            )
            
            # 如果是编织或定型工序，也发布质检任务
            if process_data.get('process_name') in ['编织', '定型']:
                print('[工序视图] 工序需要质检，自动发布质检任务')
                self.container_integration.publish_quality_task(
                    order_no=process_data.get('order_no', ''),
                    order_no=process_data.get('order_no', ''),
                    customer_name=process_data.get('customer_name', ''),
                    product_type=process_data.get('product_type', ''),
                    inspection_type='终检',
                    operator_id='OP004',
                    operator_name='质检'
                )
            
            return task_id
            
        except Exception as e:
            print(f'[工序视图] 发布任务失败: {e}')
            import traceback
            traceback.print_exc()
            return None
```

### Step 4: 在添加工序后发布任务

修改 `_add_process` 方法，在保存工序到数据库后添加：

```python
    def _add_process(self):
        """添加工序"""
        if not self.current_order_id:
            alert('请先选择订单！')
            return
        
        # ... 现有的表单创建代码 ...
        # 假设你已经保存了工序，并且有 form_data 数据
        
        # ====== 新增：发布任务到容器池 ======
        if self.container_integration and self.container_integration.is_available:
            # 获取订单和生产工单信息
            order = self._cached_order or OrderDAO.get_by_id(self.current_order_id)
            prod = self._cached_prod or ProductionDAO.get_by_order_id(self.current_order_id)
            
            if order and prod:
                task_data = {
                    'order_no': order.get('order_no', ''),
                    'order_no': prod.get('order_no', ''),
                    'process_name': form_data.get('process_name', ''),
                    'customer_name': order.get('customer_name', ''),
                    'product_type': order.get('product_type', ''),
                    'quantity': order.get('quantity', 0),
                    'unit': order.get('unit', ''),
                    'planned_qty': form_data.get('planned_qty', 0),
                    'operator_id': form_data.get('operator_id', 'OP001'),
                    'operator_name': form_data.get('worker', ''),
                }
                
                task_id = self._publish_task_to_container(task_data)
                
                if task_id:
                    print(f'[工序视图] 任务已发布到容器池: {task_id}')
                    # 可选：显示提示消息给用户
                    # messagebox.showinfo('成功', f'工序任务已发布！\n任务ID: {task_id}')
                else:
                    print(f'[工序视图] 任务发布失败')
        
        # 刷新列表
        self.load_processes()
        self._show_order_info()
```

---

## 🔧 API 参考

### DesktopContainerIntegration 类

#### 初始化

```python
from desktop_container_integration import DesktopContainerIntegration

# 使用默认路径
integration = DesktopContainerIntegration()

# 或指定路径
integration = DesktopContainerIntegration(db_path='/path/to/wechat_container.db')
```

#### publish_report_task() - 发布报工任务

```python
task_id = integration.publish_report_task(
    order_no='ORD202604001',
    order_no='WO202604001',
    process_name='编织',
    customer_name='上海机械厂',
    product_type='不锈钢编织网',
    quantity=100,
    unit='米',
    planned_qty=100,
    process_status='待开始',
    operator_id='OP001',
    operator_name='张三',
    priority='normal'
)
```

#### publish_quality_task() - 发布质检任务

```python
task_id = integration.publish_quality_task(
    order_no='ORD202604001',
    order_no='WO202604001',
    customer_name='上海机械厂',
    product_type='不锈钢编织网',
    inspection_type='终检',  # 终检/首检
    operator_id='OP004',
    operator_name='质检',
    priority='high'
)
```

#### 其他方法

```python
# 获取所有任务
tasks = integration.get_all_tasks(limit=100)

# 获取统计
stats = integration.get_task_count()
print(stats)
# {'total': 10, 'by_status': {'pending': 3, 'acknowledged': 5, 'completed': 2}}

# 打印状态
integration.show_status()
```

---

## 📊 数据结构

### 发布任务的完整数据

```python
{
    'order_no': 'ORD202604001',
    'order_no': 'WO202604001',
    'process_name': '编织',
    'customer_name': '上海机械厂',
    'product_type': '不锈钢编织网',
    'quantity': 100,
    'unit': '米',
    'planned_qty': 100,
    'process_status': '待开始',
    'operator_id': 'OP001',
    'operator_name': '张三',
    'priority': 'normal'
}
```

### 容器池存储的数据

```python
{
    'id': 'task_xxx',
    'title': '报工任务-ORD202604001-编织',
    'status': 'pending',
    'content': {
        'order_no': 'WO202604001',
        'order_no': 'ORD202604001',
        'customer_name': '上海机械厂',
        'product_type': '不锈钢编织网',
        'quantity': 100,
        'unit': '米',
        'process_name': '编织',
        'planned_qty': 100,
        'process_status': '待开始',
        'operator_name': '张三',
        'voice_text': '订单ORD202604001，工序编织，数量100米，请确认任务！'
    },
    'related_order': 'ORD202604001',
    'created_at': '2026-04-30 12:34:56'
}
```

---

## 🧪 测试流程

### 完整端到端测试

1. **启动网页端服务**
   ```bash
   cd mobile_api_ai
   python wechat_work_bot_v2.py
   ```

2. **在桌面端创建测试数据**（运行集成演示）
   ```bash
   cd /path/to/project
   python desktop_container_integration.py
   ```

3. **测试网页端扫码获取数据**
   - 打开浏览器: `http://localhost:5003`
   - 在"扫码"标签页输入: `WO:WO202604001`
   - 点击"获取任务"，应该能看到从容器池获取的任务数据

4. **确认和报工**
   - 在网页端确认任务
   - 在网页端提交报工
   - 应该能在控制台看到企业微信群通知（如果已配置）

---

## 📝 注意事项

### 目录结构

确保以下文件位置正确：

```
project/
├── desktop_container_integration.py  ← 新文件
├── process_view_integration_example.py ← 示例
├── mobile_api_ai/
│   ├── container_center_v5.py
│   ├── wechat_work_bot_v2.py
│   ├── wechat_container.db
│   └── ...
└── views/
    └── process_view.py  ← 需要修改的文件
```

### 错误处理

集成模块已包含完整的错误处理：
- 如果容器中心不可用，会自动跳过发布
- 所有异常都会被捕获并打印到控制台
- 不会影响主程序的运行

### 性能影响

- 任务发布是同步的，但很快（毫秒级）
- 不会阻塞UI太长时间
- 如果需要，可以改成异步发布

---

## 🔄 常见问题

### Q: 容器数据库在哪里？

默认在 `mobile_api_ai/wechat_container.db`

### Q: 如何修改容器数据库路径？

```python
integration = DesktopContainerIntegration(db_path='/custom/path/container.db')
```

### Q: 任务发布失败怎么办？

检查控制台输出，会有详细的错误信息。常见原因：
- `container_center_v5.py` 找不到
- 数据库文件权限问题
- 缺少依赖库

### Q: 如何检查容器池状态？

```python
integration.show_status()  # 打印到控制台
# 或
stats = integration.get_task_count()  # 获取字典
```

---

## 📞 相关文档

- `数据分层架构说明.md` - 架构说明
- `混合方案使用说明.md` - 网页端使用
- `企业微信应用机器人部署指南.md` - 应用机器人（可选）

