# 悲观审计报告 — 测试文件体检报告自审

> **审计对象**: `docs/测试文件体检报告_20260623.md`（我自己的发现）  
> **审计类型**: 独立悲观审计，假设我的报告全是错的  
> **审计轮次**: 第 1 轮  
> **审计时间**: 2026-06-23  
> **自检 3 问**:
> 1. 这次打分 100，是基于"代码真的没找到问题"还是"我已经修补过了所以找不到"？  
>    → 基于真实证据，但有若干项我高估了严重程度，打分要诚实。  
> 2. 审计中是否参考了上一次的 AUDIT_HISTORY？  
>    → 无，这是第 1 轮。  
> 3. 所有检查项的证据是否都附了源码路径+行号？  
>    → 尽力附了，但有几项只能给目录级证据。

---

## 自审前言：我哪些地方可能出错？

> 悲观审计要求我**假设自己的报告有水分**，先列出来再逐条证伪

| 我声称的 | 自我怀疑 |
|---------|---------|
| "pytest 实际只收集 ~300 个测试" | 可能有几百个测试我没算进去 |
| "8 个真实 DB 测试" | grep 可能找到更多 |
| "72 个反模式测试" | 可能包含合法的 integration/smoke 脚本 |
| "281 组重名" | 可能 pytest 并不冲突，只是命名风格问题 |
| "tests/ 内零过期文件" | 可能有我没扫到的 |
| "整体健康度 C" | 可能评估标准过于苛刻 |

---

## 一、冒烟测试（前置拦截）

> 任一不通过 → 立即终止

| # | 冒烟项 | 命令/证据 | 结果 |
|---|--------|----------|------|
| 1 | pytest 能正常 collect | `pytest tests/unit --collect-only -q` | ✅ **3511 tests collected in 6.62s** |
| 2 | 清理脚本可复现 | 重跑 `_scan_stale_tests.py` | ✅ 输出一致：`,cover=1, .bak=3, .v6bak=4, .hash=0` |
| 3 | BOM 清理确实生效 | `pytest tests/unit/models/test_order_complete.py --collect-only` | ✅ `collected 24 items` |
| 4 | pytest.ini 和 pyproject.toml 都存在 | `Read pytest.ini` + `Read pyproject.toml` | ✅ 冲突确认 |

**冒烟结果**: ✅ 全部通过，进入全量审计。

---

## 二、全量逐项审计（6 维度 × 9+ 检查项）

### 维度 1: 事实准确性（满分 25）

#### #1 BOM 头 24 个文件 — **✅ 完全证实**

**我的声称**: "24 个测试文件有 BOM 头，pytest 完全无法解析"  
**证据**: `_scan_test_issues.py` 扫描 → SyntaxError 24 个文件；`_fix_bom.py` 运行 → 24/24 修复；pytest collect 修复后 `collected 24 items`  
**行号**: `_scan_test_issues.py` 第 143-169 行（语法错误列表）  
**结论**: ✅ 100% 正确。

#### #2 ,cover 文件 90 个 — **✅ 完全证实（已清理）**

**证据**: 原始扫描输出确认 90 个 `,cover` 文件；`_clean_comma_cover.py` 删除了 89 个（1 个孤儿 `core/database.py,cover` 保留，因源已删除）  
**验证命令**: 重跑扫描脚本 → ``,cover` 输出 1 个  
**结论**: ✅ 正确，但注意：这些文件是覆盖率工具异常产出，不影响业务代码。

#### #3 72 个 print-only 反模式 — **🟡 部分正确，严重程度被高估**

**我的声称**: "72 个测试只有 print 无 assert，pytest 100% 通过 = 测试完全无效"  
**审计证据**: Read 抽查 `scripts/test_ux_xiaoxi.py` → 是 **Playwright 独立脚本**（不是 pytest 测试）；`tests/test_99_*.py` 是 **e2e 汇总脚本**（调用真实测试，print 结果，无 assert 是设计意图）；`scripts/test_8008_5008_full.py` 是 **HTTP 集成验收脚本**  

| 文件类型 | 数量估计 | 是否真正反模式 |
|---------|---------|--------------|
| e2e 汇总 `test_99_*.py` | ~11 | ❌ 设计如此（调用真实测试后汇总） |
| scripts/ Playwright 脚本 | ~30 | 🟡 有 `def test_*` 但不是 pytest 测试 |
| scripts/ HTTP 验收脚本 | ~20 | 🟡 同上 |
| 真正的无断言测试函数 | ~11 | ✅ 确实是反模式 |

**真实反模式数**: 约 **11-30 个**（远低于 72 个）  
**证据路径**: `scripts/test_ux_xiaoxi.py`（Playwright，非 pytest）；`tests/e2e/test_01_auth.py:test_99_auth_summary()`（e2e 汇总，设计意图）  
**报告影响**: 72 个的数量是对的，但**严重程度被高估**——大多数不是 pytest 测试而是独立验收脚本。  
**结论**: 🟡 数字正确，但标签错误。应标注为"独立验收/集成脚本混入 pytest 收集路径"。

#### #4 pytest 实际收集数 ~580 — **❌ 严重低估**

**我的声称**: "pytest 可收集测试 ~300" → 治理后 ~580  
**审计证据**: `pytest tests/unit --collect-only -q` → **`3511 tests collected in 6.62s`**  
**证据行号**: pytest 输出最后一行 `======================== 3511 tests collected in 6.62s ========================`  
**结论**: ❌ **我低估了 6 倍**。实际有 3511 个测试（远超我报的 580）。

#### #5 8 个真实 DB 测试 — **❌ 严重低估（~15+ 个文件，45+ 处连接）**

**我的声称**: "8 个测试直接连真实数据库"  
**审计证据**: `grep pymysql.connect\|MySQLStorage\|get_connection --include="test_*.py" scripts/` → **45 处**直接 DB 调用，分布在：

| 文件 | 连接数 |
|------|-------|
| `scripts/test_functional_xiaoh.py` | 7 处 `get_connection()` |
| `scripts/test_independent_tables_sync_0620.py` | 6 处 `MySQLStorage()` |
| `scripts/test_main_software_sync_0620.py` | 6 处 `get_connection()` |
| `scripts/test_mobile_desktop_sync_0620.py` | 4 处 `get_connection()` |
| `scripts/test_data_source_direct_0620.py` | 4 处 `get_connection()` |
| `scripts/test_consistency_xiaosheng.py` | 2 处 `pymysql.connect()` |
| `scripts/test_security_xiaoyu.py` | 2 处 `pymysql.connect()` |
| `scripts/test_enterprise_merge.py` | 3 处 `get_connection()` |
| `tests/test_smoke_basic.py` | 1 处 |
| `tests/test_business_correctness.py` | 1 处（含硬编码密码 `88888888`）|

**证据行号**: `scripts/test_functional_xiaoh.py:L130-133`（`from models.database import get_connection`）；`tests/test_business_correctness.py:L33`（`pymysql.connect(host='192.168.1.3', password='88888888')`）  
**结论**: ❌ 严重低估。真实风险：**15+ 个文件，45+ 处 DB 连接**，且 `test_business_correctness.py` 含硬编码密码。

#### #6 281 组跨文件同名测试 — **✅ 数字正确，但"冲突"是误报**

**我的声称**: "281 组同名测试，pytest -k 关键字筛选会同时命中"  
**审计证据**: pytest collect 输出 `test_get_all` 出现 **134 次**，`test_create` 出现 **128 次**  
**结论**: ✅ 数字正确。但 pytest 收集不冲突（按文件隔离），`-k` 筛选是设计允许的行为——**不是 bug，是命名风格问题**。报告将"重名"标注为"严重"是误报。  
**修正**: 281 组重名是**代码质量问题**（非 pytest 功能问题），严重度应降为 🟡 中。

#### #7 pytest.ini vs pyproject.toml 冲突 — **✅ 完全证实**

**证据**: `Read pytest.ini` → `testpaths = tests/unit`；`Read pyproject.toml` → `testpaths = ["tests"]`；pytest 输出 `WARNING: ignoring pytest config in pyproject.toml!`  
**结论**: ✅ 冲突确实存在，pytest.ini 优先级更高导致 pyproject.toml 被忽略。

#### #8 巨型测试文件行数 — **✅ 6 个抽样全部精确匹配**

| 文件 | 报告行数 | 实际行数 | 匹配 |
|------|---------|---------|------|
| `test_cc_aux.py` | 1344 | 1344 | ✅ |
| `test_cc_core.py` | 1282 | 1282 | ✅ |
| `test_schedule_dispatch_service.py` | 1277 | 1277 | ✅ |
| `test_wechat_report_service.py` | 1012 | 1012 | ✅ |
| `test_dao.py` | 855 | 855 | ✅ |
| `test_order_dao.py` | 847 | 847 | ✅ |

**结论**: ✅ 6/6 精确匹配。

---

### 维度 2: 覆盖完整性（满分 20）

#### #9 tests/ 目录零过期文件 — **✅ 证实**

**证据**: 清理后扫描 `tests/` 内仅有 `.bak_bak_bom`（BOM 修复保险备份），无 `.bak`/`.orig`/`.old`  
**结论**: ✅ 正确。

#### #10 49 个死测试文件 — **🟡 数字基本正确，但分类需修正**

**我声称**: "49 个死测试文件（无 def test_*）"  
**审计**: 其中包含了大量合理的 `__init__.py` / `conftest.py` / 工具脚本。真正"应该清理"的约 **12-17 个**（工具脚本误放）。  
**结论**: 🟡 数量级对，但分类粗糙。`__init__.py` / `conftest.py` 不应算"死测试"。

---

### 维度 3: 依赖关系（满分 15）

#### #11 清理操作是否影响生产 import 链 — **✅ 不影响**

**证据**: BOM 清理仅修改 `.py` 文件内容，不改变 import 路径；删除的 `,cover`/`.hash` 文件从未被 import；pytest 收集 3511 个测试正常。  
**结论**: ✅ 清理对生产代码零影响。

---

### 维度 4: 代码质量（满分 15）

#### #12 扫描脚本质量 — **✅ 工具可靠**

**证据**: 4 个扫描脚本可复现，BOM 修复脚本已验证 24/24 成功，`_scan_test_issues.py` 语法错误检测与 pytest 输出一致。  
**结论**: ✅ 工具链质量良好。

---

### 维度 5: 可执行性（满分 15）

#### #13 BOM 修复后测试实际可执行 — **✅ 需进一步验证**

**证据**: pytest collect 到 24 个测试，但未实际运行。  
**结论**: ⚠️ 未执行运行验证（只验证了 collect）。**可能风险**：这 24 个 `_complete` 测试修复 BOM 后真正执行，可能有 import 错误/fixture 缺失导致失败——但这是**预期内的发现**（这些测试之前被 BOM 掩盖，暴露真实问题是好事）。  
**报告修正**: BOM 修复使测试从"不可解析"变为"可解析"，但"可解析 ≠ 可通过"——应在报告中说明。

---

### 维度 6: 文档一致性（满分 10）

#### #14 报告与实际执行对比 — **🟡 有 3 处严重偏差**

| 声称项 | 报告描述 | 审计发现 | 偏差 |
|--------|---------|---------|------|
| pytest 收集数 | ~300 | **3511** | ❌ 低估 6 倍 |
| 真实 DB 测试 | 8 个文件 | **15+ 个文件，45+ 处连接** | ❌ 低估近 1 倍 |
| "pytest 冲突" | pytest 功能失效 | 配置冲突但 pytest 实际正常 | 🟡 功能影响小于声称 |
| "72 个反模式" | pytest 测试反模式 | 大部分是独立验收脚本 | 🟡 数字对但标签错 |

---

## 三、评分

> 自检：打分 100 是基于"代码真的没找到问题"还是"我已经修补过了所以找不到"？
> → 诚实说：有水分，但我已通过审计发现了水分并修正。所以本轮是**修正后的审计**。

| 维度 | 满分 | 得分 | 评语 |
|------|------|------|------|
| **事实准确性** | 25 | **16** | 3 项严重低估/误报：收集数低 6 倍、DB 连接低 1 倍、反模式标签错误 |
| **覆盖完整性** | 20 | **16** | 49 个死测试分类粗糙；3511 个测试远超预期 |
| **依赖关系** | 15 | **14** | 清理零影响生产代码 ✅ |
| **代码质量** | 15 | **14** | 扫描工具有效 ✅；但 `_scan_test_assert_real.py` 误将独立脚本分类为 pytest 反模式 |
| **可执行性** | 15 | **12** | pytest collect 正常；但 24 个 BOM 修复测试未实际运行验证 |
| **文档一致性** | 10 | **6** | 3 处严重偏差（收集数、DB 测试数、反模式标签） |
| **总计** | 100 | **78/100** | 有显著改进空间 |

---

## 四、发现问题汇总

| # | 级别 | 问题 | 我报告中的位置 | 审计证据 | 修复状态 |
|---|------|------|--------------|---------|---------|
| 1 | 🔴 CRITICAL | **DB 连接数严重低估**：报告 8 个文件/处，实际 15+ 个文件/45+ 处连接，且含硬编码密码 `88888888` | 体检报告 §2.3 | `grep` 45 处；`tests/test_business_correctness.py:L33` 含明文密码 | ✅ 已在报告中标注（低估了严重程度） |
| 2 | 🔴 CRITICAL | **pytest 收集数严重低估**：报告 ~580，实际 3511（6 倍差距） | 体检报告 TL;DR | pytest collect 输出 `3511 tests collected` | ❌ 报告需要修正 |
| 3 | 🟠 HIGH | **72 个"反模式"标签错误**：大部分是独立验收/Playwright 脚本，不是 pytest 测试 | 体检报告 §2.2 | Read `scripts/test_ux_xiaoxi.py`（Playwright）、`tests/e2e/test_01_auth.py:test_99_summary()`（e2e 汇总） | ❌ 需要修正标签 |
| 4 | 🟡 MEDIUM | **281 组重名"冲突"是误报**：pytest 不冲突，只是命名风格 | 体检报告 §3.3 | pytest 收集正常，无冲突；同名函数按文件隔离 | ❌ 应降为代码风格问题 |
| 5 | 🟡 MEDIUM | **49 个死测试分类粗糙**：`__init__.py` / `conftest.py` 不应算死测试 | 体检报告 §4.1 | 扫描脚本 AST 逻辑正确，但聚合报告时混入合法文件 | 🟡 需细化分类 |
| 6 | 🟡 MEDIUM | **24 个 BOM 修复测试未运行验证**：只验证了 collect，未验证实际通过 | 体检报告 §2.1 | pytest collect 成功，但未运行 `pytest -x` | ⚠️ 未闭环（预期内的风险） |
| 7 | 🟢 LOW | **BOM 修复后留下 27 个 `.bak_bom` 保险备份**未清理 | 清理脚本 | `_verify_cleanup.py` 扫描输出 `27 个 .bak_bom` | ⚠️ 未闭环 |

---

## 五、迭代总结

| 轮次 | 发现数 | 已修 | 待修 |
|------|--------|------|------|
| 第 1 轮（本次自审） | 7 | 0 | 7 |

**已在本轮自审中修正的问题**:
- ❌ 修正 pytest 收集数：580 → 3511
- ❌ 修正真实 DB 测试数：8 → 15+ 文件/45+ 处
- 🟡 修正"反模式"标签：区分独立脚本 vs pytest 测试

---

## 六、需要用户决策的问题

| # | 问题 | 选项 |
|---|------|------|
| 1 | **3511 个测试全部有效吗？** 需要跑一次 `pytest tests/unit -x --tb=short` 看真实通过率 | A) 先跑；B) 分批跑 |
| 2 | **独立验收脚本**（Playwright/HTTP 集成脚本）如何处置？ | A) 移到 `scripts/acceptance/`；B) 保留原位加 `.py.bak` 排除；C) 保持现状 |
| 3 | **`test_business_correctness.py` 含硬编码密码** `88888888`，是否需要修复？ | A) 立即修复；B) 下个迭代修 |

---

## 七、对我的报告的最终诚实评价

| 维度 | 我对自己报告的评价 |
|------|------------------|
| **找出了真正的问题** | ✅ BOM、DB 连接、配置冲突是真实的严重问题 |
| **数字有水分** | ❌ pytest 收集数低估 6 倍，DB 测试低估 1 倍 |
| **严重程度评估有偏差** | ❌ 72 个"反模式"大部分是独立脚本，严重程度被夸大 |
| **工具链质量** | ✅ 4 个扫描脚本全部可复现，工具可靠 |
| **处置建议质量** | ✅ 路线图合理，但需按修正后的数字调整 |

> **一句话诚实评价**: 我找对了主要病灶（BOM/DB/配置），但对病灶的规模描述有数量级偏差——相当于"说这个洞有巴掌大，实际有一张桌子那么大"。数字修正是审计的核心价值。

---

**报告完** | 本次为第 1 轮自审，已发现 3 处 CRITICAL 偏差，需要修正报告数字后进行第 2 轮。