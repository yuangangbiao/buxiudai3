# 业务影响报告 - 测试文件体检与清理（2026-06-23）

> **触发场景**: 测试体检 Phase 1/2/3 完成，用户询问"进行abc"和"先做1"后续操作
> **执行日期**: 2026-06-23
> **产出**: 体检报告 + 清理报告 + 悲观审计

---

## 1. 用户场景对比

> 改善前（痛点） → 改善后（价值）

| # | 用户角色 | 改善前（痛点） | 改善后（价值） |
|---|---------|---------------|---------------|
| 1 | 测试工程师 | 27 个测试文件含 BOM 头，pytest 静默跳过，BOM 修复后恢复 52 个测试 | 所有测试文件 UTF-8 无 BOM，`_complete` 系列测试可被 pytest 正常收集和执行 |
| 2 | 开发人员 | pytest 配置冲突（pytest.ini vs pyproject.toml），覆盖率配置互相覆盖 | 统一为 pyproject.toml，无警告，`--cov-fail-under=55` 门槛设置生效 |
| 3 | 测试工程师 | 90 个异常 `,cover` 文件散布仓库，容易误用过期备份 | 89/90 已删除，仅剩孤儿 `core/database.py,cover` 等待确认删除 |
| 4 | 开发人员 | 49 个"死测试"占用 tests/ 目录（utility 脚本/调试探针/一次性报告），pytest 索引污染 | 分类清理：utility 移 `scripts/tools/`、验收脚本归 `scripts/manual_acceptance/`、真正死测试删除 |
| 5 | 测试工程师 | 8 个旧覆盖率数据目录（8.79 MB，2026-06-04 过期）产生误导性覆盖率报告 | 删除 `coverage_html/` + `.coverage` + `coverage.xml`，下次跑 `--cov` 自动生成最新数据 |
| 6 | CI/CD | 3511 个测试可收集但配置冲突无法正确执行 | 3563 个测试正常收集（BOM 修复后 +52），配置统一，CI 管道可正常工作 |
| 7 | 安全合规 | 测试文件含明文密码 `password='88888888'` | 改为 `os.environ.get('TEST_DB_PASSWORD')`，无密码时 RuntimeError 提示 |

---

## 2. 业务能力新增

> 按业务流分类

| 业务流 | 新增/优化功能 | 影响范围 |
|--------|--------------|---------|
| 测试基础设施 | pytest 配置统一（pyproject.toml） | 全项目所有测试 |
| 测试基础设施 | BOM 头修复（27 文件 → 0） | 24 个 `_complete` 测试恢复执行 |
| 测试基础设施 | 测试标记规范化（integration/manual/slow） | 4 个 DB 测试 + 15 个 scripts 测试 |
| 测试质量 | 过期覆盖率数据清理 | 8.79 MB 空间释放，下次生成最新数据 |
| 测试组织 | 死测试分类清理（49 个） | 32 个文件分类归档，pytest 索引净化 |
| 测试安全 | 明文密码 → 环境变量 | `tests/test_business_correctness.py` 安全合规 |

---

## 3. 不变更部分

> 防回归保护清单（哪些保持原样，行为零变更）

| # | 模块/功能 | 保护措施 | 验证方式 |
|---|----------|---------|---------|
| 1 | `tests/unit/` 单元测试 | 未修改源码，仅调整目录结构 | `pytest tests/unit --collect-only` → 3563 tests |
| 2 | `tests/e2e/` 端到端测试 | 已在 pyproject.toml ignore 列表 | 默认不收集，不影响 |
| 3 | `tests/integration/` 集成测试 | 已在 pyproject.toml ignore 列表 | 默认不收集，需手动跑 |
| 4 | `mobile_api_ai/` 核心服务代码 | 未修改任何源码 | 所有服务保持原状 |
| 5 | `desktop/` 桌面端代码 | 未修改任何源码 | 所有视图/Presenter 保持原状 |
| 6 | `models/` 数据模型 | 未修改任何源码 | 所有 DAO/Model 保持原状 |
| 7 | `core/` 核心组件（除测试外） | 未修改任何源码 | EventBus/CircuitBreaker/Config 保持原状 |

---

## 4. 一句话总结

> 本次改动让测试工程师的 pytest 体验从"配置冲突、27 个 BOM 跳过、49 个死文件污染、8.79 MB 过期数据"的混乱状态，变为"配置统一、BOM 清零、测试文件结构清晰、覆盖率数据可信任"的规范化状态。

---

## 5. 量化指标

| 指标 | 清理前 | 清理后 | 变化 |
|------|--------|--------|------|
| 含 BOM 的 .py 文件 | 27 | 0 | -27 |
| `,cover` 异常备份 | 90 | 1 | -89 |
| `.hash` 文件 | 9 | 0 | -9 |
| 过期覆盖率数据 | 8.79 MB | 0 | -8.79 MB |
| pytest collect 数 | 3511 | 3563 | +52（BOM修复恢复） |
| 死测试文件处置 | 0 | 49 | +49 |
| integration marker | 0 | 6+4=10 | +10 |
| manual marker | 0 | 5+6=11 | +11 |
| pytest.ini 冲突 | 存在 | 已删除 | ✅ |
| 明文密码文件 | 1 | 0 | ✅ 已修复 |

---

**报告日期**: 2026-06-23
**执行人**: AI 结对编程助手 (TRAE)
