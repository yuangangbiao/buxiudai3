# P0 修复待办清单 - 2026-06-23

> 执行人: 小曦 (产品经理)
> 输入: 3 位专家的残留风险 + 后续 P1 任务
> 状态: P0 已闭环, 此清单为 P1 输入

---

## A. P0 残留风险（必须主动暴露, 不可藏）

| # | 风险 | 来源 | 影响 | 建议 P1 任务 |
|---|------|------|------|------------|
| R1 | `_TASK_TYPE_TABLE_MAP` 还有 6 个 key 未在 get_packages 注册: `quality_inspection` / `quality_task` / `material` / `material_pickup` / `repair` / `outsource` | 小贺 (P0-2 修复时发现) | 这 6 类任务用 `get_packages` 拉取仍返 `[]`, 静默丢失, 客户端表现"查无数据" | P1-T1 |
| R2 | 5003 服务已重启但 role 字段运行时验证未做 (E2E D-5 skip) | 小钰 (P0-6 修复时发现) | 代码已改, 但 5003 health 路径不匹配 (`/api/dispatch-center/health` 404), 端到端未跑通 | P1-T2 |
| R3 | 8008 sync_bridge 已启动, 但 Δ 同步量监控未做 (无 requests/min, errors/min, Δ rows/min) | 小钰 (启动确认时发现) | 8008 可能空跑/异常跑, 业务方无感 | P1-T3 |
| R4 | 5001 鉴权已修, CSRF 完整性 e2e 未做 (仅 4 条 pytest 静态校验, 缺 X-CSRF-Token 头自动注入) | 小钰 (P0-6 验收时发现) | 登录后前端 fetch 缺 X-CSRF-Token 头, 写操作可能 403 | P1-T4 |
| R5 | session token 等价于明文 `uid:uname` 编码, 无签名/无过期 | 小圣 (P0-4 修复时识别) | 拿到 session cookie 即可伪造请求, 仅靠 HttpOnly/SameSite 兜底 | P1-T5 |
| R6 | 测试用户兜底 role 改为 'admin' (便利测试) | 小钰 (P0-6 修复时识别) | 生产环境测试账户被攻破=admin 权限, 应加白名单 | P1-T6 |
| R7 | 全局无限流/审计日志 (DoS / 暴力破解防护缺失) | 小钰 (P0-6 总结) | 登录/写接口可被刷 | P1-T7 |

---

## B. P1 任务清单（按优先级排序）

### P1-T1 修完 6 个 get_packages 残留分支 (R1)

- **目标**: `mobile_api_ai/storage/mysql_storage.py:1104-1148` `get_packages` 增加 6 个 data_type 分支
- **影响表**: quality_records (新增 quality_inspection/quality_task) + material_records (新增 material/material_pickup) + repair_records (新增 repair) + outsource_records (新增 outsource)
- **验收**: pytest 覆盖 6 个分支 + E2E 真实 MySQL 调用返 >0 行
- **负责人**: 小贺
- **估算**: 1 天

### P1-T2 5003 health 端点路径修正 + D-5 端到端验证 (R2)

- **目标**: `mobile_api_ai/standalone_dispatch_server.py` 增加 `/api/dispatch-center/health` 端点 (或修正 E2E 脚本调用路径)
- **验收**: E2E D-5 不再 skip, 真实 mobile_login 返真实 role (非 'worker' 硬编码)
- **负责人**: 小钰
- **估算**: 0.5 天

### P1-T3 8008 同步链路监控埋点 (R3)

- **目标**: `mobile_api_ai/sync_bridge.py` (8008 端口) 增加 `requests_total` / `errors_total` / `delta_rows_total` Prometheus 指标
- **验收**: 启动 8008 后, 访问 `/metrics` 端点可看到 3 类指标, 数值随同步业务更新
- **负责人**: 小圣 (架构)
- **估算**: 1 天

### P1-T4 5001 CSRF Token 头自动注入 (R4)

- **目标**: `desktop_web/templates/login.html` + 全局 fetch 拦截器, 自动注入 `X-CSRF-Token: <session.csrf_token>`
- **验收**: 前端任意写操作 fetch 自动带 X-CSRF-Token 头, 端到端 200 (非 403)
- **负责人**: 小钰
- **估算**: 0.5 天

### P1-T5 升级 5001 token 为 JWT 签名 (R5)

- **目标**: `desktop_web/server.py:619-643` api_login 改用 PyJWT 签发 token (对齐 5003 `mobile_api/auth.py` 已有 JWT 实现)
- **验收**: token 含过期时间 + 签名, 篡改后 5003 拒签返 401
- **负责人**: 小圣
- **估算**: 1.5 天

### P1-T6 测试用户白名单 + 角色限制 (R6)

- **目标**: `mobile_api_ai/standalone_dispatch_server.py` 增加白名单配置 `ALLOWED_ADMIN_USERS`, '测试' 账户不允许 admin
- **验收**: '测试' 账户登录返 role='viewer' (非 'admin'), admin 路径 403
- **负责人**: 小钰
- **估算**: 0.5 天

### P1-T7 全局限流 + 审计日志 (R7)

- **目标**: Flask-Limiter 限流 `/api/login` (10/min/IP) + 关键写操作审计日志 (who/when/what)
- **验收**: 同一 IP 1 分钟内 11 次登录, 第 11 次 429; 写操作落 audit_log 表
- **负责人**: 小钰
- **估算**: 2 天

### P1-T8 5001 ↔ 5003 双 token 协议 (内部 + 颁发)

- **目标**: 5001 内部继续用 base64(uid:uname), 5003 颁发正式 JWT, 5001 代理转发时使用 5003 颁发的 JWT
- **验收**: 5001→5003 调用使用 5003 颁发的 JWT, 含签名+过期, 5003 鉴权一致
- **负责人**: 小圣
- **估算**: 2 天

### P1-T9 5xxx 服务健康检查端点统一

- **目标**: 5001/5003/5008/8008 全部实现 `/api/<service>/health` 端点, 返 200 + 服务名 + DB 连接状态
- **验收**: 4 个服务 health 端点全部 200, E2E 脚本可批量调用
- **负责人**: 小圣
- **估算**: 1 天

### P1-T10 数据库合并决策 (8008 稳定后)

- **目标**: 当前 desktop_web (5001) 用桌面端 MySQL, mobile_api_ai (5008) 用云端 MySQL, Δ 同步维护成本高. 待 8008 稳定 1 个月后, 评估合并为单库
- **前置**: P1-T3 监控就绪, 跑 1 个月数据
- **验收**: 决策文档 `docs/db_merge_decision.md` 含优劣对比 + 推荐方案
- **负责人**: 小圣
- **估算**: 决策 1 周, 实施 1 周

---

## C. 总工作量估算

| 类别 | 任务数 | 总估算 |
|------|-------|--------|
| P1 修复 (T1~T7) | 7 | 7 天 |
| P1 架构升级 (T8~T9) | 2 | 3 天 |
| P1 决策 (T10) | 1 | 2 周 (含 1 月观察期) |
| **合计** | **10** | **约 2 周 + 1 月观察期** |

---

## D. 紧急程度排序 (1=最紧急)

1. **P1-T1** (6 个 get_packages 静默丢数据, 客户可能感知)
2. **P1-T4** (CSRF 头缺失, 前端写操作 403 影响面广)
3. **P1-T2** (5003 role 端到端验证, P0 闭环最后一公里)
4. **P1-T3** (8008 监控, 避免空跑不被发现)
5. **P1-T6** (测试账户 admin 风险)
6. **P1-T7** (限流/审计, 安全加固)
7. **P1-T5** (token 升级, 安全深化)
8. **P1-T8** (双 token 协议, 架构升级)
9. **P1-T9** (健康检查, 运维便利)
10. **P1-T10** (DB 合并决策, 长期)

---

## E. 资源协调建议

- **小贺**: P1-T1 (1 天, 与质检/物料/外协业务理解对接)
- **小钰**: P1-T2 / T4 / T6 / T7 (合计 3.5 天, 安全闭环)
- **小圣**: P1-T3 / T5 / T8 / T9 / T10 (合计 5.5 天 + 1 月观察, 架构升级)
- **小曦 (PM)**: 协调 + 业务影响报告 + 验收签字 (贯穿全程)
