# 修改文件对应细分方案
> 日期: 2026-05-30 | 涉及: 14源码 + 5文档

---

## 一、存储层 (3 文件)

### 1. `构想文件/wechat_container数据库MySQL迁移方案/mysql_storage.py`

| 改动 | 行 | 影响 |
|------|-----|------|
| `PREFIX='cc_'` → `''` | 78 | 匹配 migration 脚本无前缀表名 |
| `load_enterprise_structure()` 加 JSON 解析 | 695 | departments/users/operators 列自动 `json.loads` |
| `get_attendance_by_date()` 新增 | 675 | 报工看板调用的考勤接口 |
| `load_dotenv` 绝对路径 | 32 | 确保读 `.env` 密码 |

### 2. `mobile_api_ai/storage_layer.py`

| 改动 | 行 | 影响 |
|------|-----|------|
| `StorageFactory.MYSQL` import path → 构想文件 | 4482 | 统一加载 MySQLStorage |
| `create_storage()` import path → 构想文件 | 4553 | 同上 |
| 删除 `mobile_api_ai/mysql_storage.py` 复制 | — | 单点维护 |

### 3. `.env`

| 改动 | 影响 |
|------|------|
| `INVENTORY_API_URL:5004` → `:5010` | 库存端口对齐 |

---

## 二、服务启动层 (2 文件)

### 4. `server_launcher.py`

| 改动 | 影响 |
|------|------|
| 库存管理加入主服务列表 (port 5010) | UI 卡片 |
| 人脸扫描移入独立服务区 | UI 解耦 |
| `_stop_all_servers` / `_heartbeat_check` 加库存 | 运维全覆盖 |

### 5. `requirements.txt`

| 改动 | 影响 |
|------|------|
| 版本全面更新对齐实际安装 | 依赖声明准确 |
| 删除 `mysql-connector-python` (pymysql替代) | 去冗余 |
| 删除 `dashscope`/`responses`/`pyzbar` | 精简 |

---

## 三、流程引擎层 (2 文件)

### 6. `mobile_api_ai/container_center_api.py`

| 改动 | 行 | 影响 |
|------|-----|------|
| `schedule_publish()` 新增 `material_purchase`/`quality`/`repair` 步骤定义 | 975-995 | 5流程完整 |
| `flow_type` 优先读请求体字段 | 964 | 桌面端直接传流程类型 |
| 回退查 `product_flow_map` | 966-974 | 旧桌面兼容 |
| `process_type` 改用实际 `flow_type` | 1017 | 不再硬编码 `'production'` |
| `ContainerCenter()` 简化 | 165 | 去掉冗余 db_path |

### 7. `mobile_api_ai/dispatch_center.py`

| 改动 | 行 | 影响 |
|------|-----|------|
| `match_flow_type()` 优先显式 `flow_type` | 370 | 调度不猜流程 |
| `FLOW_MATCHING_RULES_DEFAULT` 精简为4条 | 344-355 | 产品/物料/质检/维修 |
| `PRODUCT_TYPE_NAMES` 映射表 | 354-361 | 产品ID→中文名 |
| `_preload_operators_from_mysql()` | 932 | 启动加载操作员 |
| 云端同步后 `invalidate_operator_cache()` | 2250 | 自动推送 |
| `DocumentStore(router=None)` 兼容修复 | 734,4680 | 启动不崩溃 |
| 删除 `sys.path` 注入 `表格机器人\本地` | 37-39 | 模块隔离 |
| 注释更新 `wechat_container.db` | 2363 | 文档准确 |

---

## 四、硬编码清理 (6 文件)

### 8. `mobile_api_ai/api/scan.py`

| 改动 | 影响 |
|------|------|
| `'type':'sqlite'` → `os.getenv('CONTAINER_STORAGE_TYPE','mysql')` | 自动选 MySQL |

### 9. `mobile_api_ai/container/task_pool.py`

| 改动 | 影响 |
|------|------|
| 同上 + 补 `import os` | |

### 10. `mobile_api_ai/container_api_server.py`

| 改动 | 影响 |
|------|------|
| 同上 + 删除 `DB_PATHS['container_storage']` + `consult_storage.db` | 全走 MySQL |

### 11. `mobile_api_ai/container_dashboard.py`

| 改动 | 影响 |
|------|------|
| `'type':'sqlite'` → `os.getenv(...)` + 删除 `wechat_container` 路径 | |

### 12. `mobile_api_ai/wechat_server.py`

| 改动 | 影响 |
|------|------|
| 同上 + 删除 `表格机器人\本地` sys.path 注入 | 模块隔离 |

### 13. `mobile_api_ai/wechat_work_bot_bp.py`

| 改动 | 影响 |
|------|------|
| 删除 `DB_PATHS['wechat_container']` 默认值 | |

---

## 五、辅助层 (3 文件)

### 14. `mobile_api_ai/api/legacy_routes.py`

| 改动 | 行 | 影响 |
|------|-----|------|
| `ContainerCenter()` 去掉 `type:sqlite` | 64 | 手机报工走 MySQL |
| 诊断路由旧路径清理 | 948,962 | 不误导 |

### 15. `mobile_api_ai/sync/handlers/sub_step_handler.py`

| 改动 | 影响 |
|------|------|
| 注释 `wechat_container.db` → `container_center MySQL` | 文档 |

### 16. `mobile_api_ai/deploy_output/.env`

| 改动 | 影响 |
|------|------|
| `CONTAINER_STORAGE_TYPE=sqlite` → `mysql` | 部署包配置 |

---

## 六、文档输出 (5 文件)

| 文件 | 内容 |
|------|------|
| `代码审查报告/flow_方案细则_v2.0.md` | 整体架构方案 v2.3 |
| `代码审查报告/API_方案细则_v2.0.md` | 206端点完整API |
| `代码审查报告/API数据源分析_20260530.md` | 数据源分析 |
| `代码审查报告/API服务端口映射_20260530.md` | 端口映射 |
| `代码审查报告/全局API清单_20260530.md` | 全局清单 |

---

## 七、改动统计

| 类别 | 文件数 | 改动量 |
|------|--------|--------|
| 存储层 | 3 | PREFIX+JSON+路径 |
| 服务启动 | 2 | UI+依赖 |
| 流程引擎 | 2 | 5流程+操作员 |
| 硬编码清理 | 6 | sqlite→mysql |
| 辅助层 | 3 | 清理+文档 |
| 文档 | 5 | 方案+API |
| **总计** | **21** | **0生产阻塞** |

---

## 八、影响矩阵

| 改动 | 服务重启 | 测试 | 数据库 |
|------|---------|------|--------|
| 流程引擎 | ✅ 需要 | 1319 passed | 只读 |
| 硬编码清理 | ✅ 需要 | 1319 passed | 无 |
| PREFIX | ✅ 需要 | 1319 passed | 无 |
| 服务启动 | ✅ 需要 | — | 无 |
| 文档 | ❌ 不需要 | — | — |
