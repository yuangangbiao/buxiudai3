# TASK: 阶段五 - 验收归档

## 元信息

| 字段 | 值 |
|------|-----|
| 任务ID | TASK-V1 |
| 所属阶段 | 阶段五（第10天） |
| 前置依赖 | 阶段四（性能与稳定性完成） |
| 后置任务 | 无（最终阶段） |

---

## 子任务清单

### V1.1 全量测试回归

| 属性 | 内容 |
|------|------|
| **描述** | 运行完整 pytest 套件，确保零失败 |
| **前置条件** | 所有阶段工作完成 |
| **验收标准** | `pytest` 全部通过，`coverage` ≥ 40% |
| **命令** | `cd mobile_api_ai && pytest --cov=. --cov-fail-under=40 -v` |

### V1.2 代码审查（自检）

| 属性 | 内容 |
|------|------|
| **描述** | 运行 flake8/black/isort 审查，确保零报错 |
| **前置条件** | 阶段一工具链配置完成 |
| **验收标准** | `flake8 .` 零报错，`black --check .` 零报错，`isort --check .` 零报错 |

### V1.3 验收文档编写

| 属性 | 内容 |
|------|------|
| **描述** | 编写 ACCEPTANCE_全面优化方案.md，记录完成情况和遗留问题 |
| **前置条件** | 所有测试和审查通过 |
| **验收标准** | ACCEPTANCE 文档完成，覆盖所有设计目标 |

### V1.4 构想→现实文件切换

| 属性 | 内容 |
|------|------|
| **描述** | 将 `d:\yuan\构想文件\全面优化方案\` 中的文档移动到 `d:\yuan\现实文件\全面优化方案\` |
| **前置条件** | 验收确认完成 |
| **验收标准** | 构想文件已移动到现实文件目录 |

---

## 最终验收清单

- [ ] `pytest` 全部通过
- [ ] `coverage` ≥ 40%
- [ ] `flake8 .` 零报错
- [ ] `black --check .` 零报错
- [ ] `isort --check .` 零报错
- [ ] 生产代码无 `print()` 残留
- [ ] 全局无 `except:` 裸异常
- [ ] CI 流水线全部通过
- [ ] 全部 5 个阶段的交付物清单完成
- [ ] ACCEPTANCE 文档完成
- [ ] 构想文件→现实文件切换完成
