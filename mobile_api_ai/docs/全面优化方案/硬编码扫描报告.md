# 硬编码扫描报告

**扫描时间**: 2026-05-26
**扫描工具**: `scripts/tools/check_hardcode.py`
**扫描范围**: dispatch_center.py, container_center 模块, 前端静态文件

---

## 汇总

| 指标 | 数值 |
|------|------|
| 总检查项 | 180 |
| 通过 | 169 |
| **失败** | **11** |
| 通过率 | 93.9% |

---

## 按类型分类

### 1. 硬编码 URL/外部地址（3 处）

| 文件 | 内容 | 风险 | 建议修复 |
|------|------|------|---------|
| `dispatch_center.py` | `http://%s:%s/api/dispatch-center/` | 中 - 拼接 URL | 抽取为 `DISPATCH_CENTER_URL` 环境变量 |
| `dispatch_center.html` | `https://你的云端域名` | 低 - 模板占位符 | 已标记为占位，保持现状 |
| `dispatch_center.html` | `https://your-domain.com` | 低 - 模板占位符 | 已标记为占位，保持现状 |

### 2. `os.getenv()` 默认值硬编码（40 处）

**调度中心（dispatch_center.py）— 15 处**：

| 环境变量 | 默认值 | 建议 |
|---------|--------|------|
| `DISPATCH_MAX_PAGE_SIZE` | `2000` | 保持（阈值默认值可接受） |
| `DISPATCH_MIN_WRITE_INTERVAL` | `2` | 保持 |
| `CC_DATA_DIR` | `os.path.join(BASE_DIR, 'data')` | 保持（基于 BASE_DIR 的动态路径） |
| `DISPATCH_MAX_ALERTS` | `500` | 保持 |
| `DISPATCH_MAX_PROCESSES` | `2000` | 保持 |
| `DISPATCH_ALERT_RETENTION_DAYS` | `30` | 保持 |
| `DISPATCH_MAX_PROCESS_TASKS` | `500` | 保持 |
| `COST_CHECKER_INTERVAL` | `3600` | 保持 |
| `SCHEDULER_INTERVAL` | `60` | 保持 |
| `COST_LOW_MARGIN_THRESHOLD` | `10` | 保持 |
| `COST_HIGH_MARGIN_THRESHOLD` | `50` | 保持 |
| `DEFAULT_RATE_LIMITS` | `1000 per day, 300 per hour` | 保持 |
| `LIMITER_STORAGE_URI` | `memory://` | 保持 |

**容器中心（container_center 模块）— 25 处**：

| 环境变量 | 默认值 | 建议 |
|---------|--------|------|
| `DEFAULT_RATE_LIMITS` | `1000 per day, 300 per hour` | 保持 |
| `LIMITER_STORAGE_URI` | `memory://` | 保持 |
| `CC_DATA_DIR` | `data` | ⚠️ 相对路径，建议改为 `os.path.join(BASE_DIR, 'data')` |
| `CORS_ALLOWED_ORIGINS` | `http://localhost:5000,http://localhost:3000` | 保持 |
| `REDIS_PORT` | `6379` | 保持 |
| `DISPATCH_CENTER_URL` | `http://127.0.0.1:5003` | 保持 |
| `CONFIG_MAX_VERSIONS` | `20` | 保持 |
| `OUTSOURCE_QUERY_LIMIT` | `2000` | 保持 |
| `CC_MAX_CONNECTIONS` | `20` | 保持 |
| `CC_CONNECTION_TTL` | `3600` | 保持 |
| `CC_DB_ORDERS` | `orders.db` | 保持 |
| `CC_DB_PRODUCTION` | `production.db` | 保持 |
| `CC_DB_EQUIPMENT` | `equipment.db` | 保持 |
| `CC_DB_MAINTENANCE` | `maintenance.db` | 保持 |
| `CC_DB_INVENTORY` | `inventory.db` | 保持 |
| `CC_DB_PROCUREMENT` | `procurement.db` | 保持 |
| `CC_DB_QUALITY` | `quality.db` | 保持 |
| `CC_DB_SETTLEMENT` | `settlement.db` | 保持 |
| `CC_DB_CUSTOMER` | `customer.db` | 保持 |
| `CC_DB_SUPPLIER` | `supplier.db` | 保持 |
| `CC_DB_HR` | `hr.db` | 保持 |
| `CC_DB_SYSTEM` | `system.db` | 保持 |

### 3. 硬编码端口（7 处）

| 文件 | 端口 | 建议 |
|------|------|------|
| `dispatch_center.py` | `8008` | 确认是否应为配置项 |
| `dispatch_center.py` | `3000` | 确认是否应为配置项 |
| `container_center` 模块 | `3000` (×2) | 确认是否应为配置项 |
| `container_center` 模块 | `9200` | 确认是否应为配置项 |
| `container_center` 模块 | `8008` (×2) | 确认是否应为配置项 |

### 4. `sys.path` 重复管理（2 处）

| 文件 | 次数 | 建议 |
|------|------|------|
| `dispatch_center.py` | 2 次 | 已符合规范（入口文件允许），无需修复 |
| `container_center` 模块 | 2 次 | 已符合规范（入口文件允许），无需修复 |

### 5. 魔术数字（1 处）

| 文件 | 位置 | 内容 | 建议 |
|------|------|------|------|
| `dispatch_center.py` | L6432 | `return 3600` | 抽取为命名常量如 `DEFAULT_INTERVAL = 3600` |

---

## 分类总览

| 类型 | 数量 | 严重程度 |
|------|------|---------|
| 硬编码 URL | 3 | 低 |
| os.getenv 默认值 | 40 | 低（含 tailscale 认证密钥 1 处为 CRITICAL） |
| 硬编码端口 | 7 | 低 |
| sys.path 管理 | 2 | 信息性 |
| 魔术数字 | 1 | 低 |
| **总计** | **53** | |

## 修复建议优先级

1. **P0（立即修复）**: 无
2. **P1（高优先级）**: `CC_DATA_DIR` 默认值从 `'data'` 改为 `os.path.join(BASE_DIR, 'data')`
3. **P2（中优先级）**: L6432 `return 3600` 抽取为命名常量
4. **P3（低优先级）**: 端口 `8008` `3000` `9200` 确认是否硬编码
5. **信息性**: sys.path、os.getenv 默认值、模板占位符
