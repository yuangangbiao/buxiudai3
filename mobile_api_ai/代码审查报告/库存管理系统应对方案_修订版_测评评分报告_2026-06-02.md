# 库存管理系统应对方案 — 修订版测评评分报告

> 测评日期：2026-06-02
> 测评范围：`库存管理系统应对方案_修订版_2026-06-02.md` + `TASK_库存系统安全加固.md`
> 测评方法：逐项对照源代码交叉验证，多维评分
> 前置文档：`库存管理系统严格测评报告_2026-05-30.md`、`库存管理系统应对方案_最差评测_2026-06-02.md`

---

## 一、综合评分

| 维度 | 权重 | 得分 | 评级 |
|------|------|------|------|
| 事实准确性 | 25% | **18/25** | ⚠️ B- |
| 覆盖完整性 | 20% | **17/20** | ✅ B+ |
| 依赖关系合理性 | 15% | **11/15** | ✅ B |
| 代码质量 | 15% | **13/15** | ✅ B+ |
| 可执行性 | 15% | **13/15** | ✅ B+ |
| 文档一致性 | 10% | **7/10** | ⚠️ B- |
| **总分** | **100%** | **79/100** | **⚠️ B** |

### 评级说明

| 评级 | 区间 | 说明 |
|------|------|------|
| ✅ A (90-100) | 优秀，可直接执行 | — |
| ✅ B (70-89) | 良好，修正小问题后执行 | 本次 79 分 |
| ⚠️ C (50-69) | 需重大修订后方可执行 | — |
| ❌ D (<50) | 不可执行，需推倒重来 | — |

---

## 二、各维度详细评分

### 2.1 事实准确性 — 18/25 ⚠️

**得分依据**：修订版相比原版 8 项事实错误，已修正 7 项（连接池、蓝图等已删除），剩余 1 项未完全修复 + 1 项新发现。

#### 已正确修正（+7 项通过）

| 编号 | 修正项 | 验证结果 |
|------|--------|----------|
| S-10 删除 | 连接池已存在，任务删除 | ✅ `db_utils.py` 确实有 `PooledDB` 连接池 |
| S-14 删除 | 蓝图已正确注册 | ✅ `routes.py` 正确注册 `web_bp` |
| S-06 | web端缺锁、API端已有FOR UPDATE | ✅ API端 L136 `FOR UPDATE` 存在，web端无锁 |
| S-08 | 优先级调整为 P1（部分路由已有基本校验） | ✅ 部分路由有 `if not product_id` 等基本校验 |
| S-09 | Blueprint已被`@before_request`保护 | ✅ L23 `check_auth` 白名单含 `/api/`，蓝图路由受保护 |
| S-13 | 从 P2 提前到 P1，仅做写操作埋点 | ✅ `operation_logs` 表和日志页面存在，确为埋点不足 |
| S-10(新) | inventory_api_server.py硬编码密码 | ✅ L11-L12 确认有默认值 |

#### 未完全修复的事实错误（−5 分）

| # | 问题 | 严重性 | 详细说明 |
|---|------|--------|----------|
| F-01 | **TASK-001 声称 base_add 有 SQL 注入，实际不存在** | 🔴 **CRITICAL** | 源代码 `routes_data.py:80` 中，`INSERT INTO ` 的表名来自白名单字典 `tables`，字段是硬编码的 `code, name, contact`，**用户输入无法控制表名或字段名**。原始代码等同于：`execute("INSERT INTO warehouses (code, name, contact) VALUES (%s,%s,%s)", …)` — 参数化查询，无注入风险。<br/><br/>方案将其标为 P0 CRITICAL 是错误的。TASK-001 实质是"添加动态字段支持的功能增强"，而非漏洞修复。 |
| F-02 | **依赖图 S03 → S04 不合理** | 🟠 HIGH | 修订版方案 mermaid 图中显示 `S03 --> S04`（路径遍历→命令注入），但两者都是 `routes_system.py` 中的**独立函数**：`backup_create`（S-04）和 `backup_restore`（S-03）。修复路径遍历对修复命令注入没有帮助，可以完全并行执行。 |
| F-03 | **"已有6+路由有参数校验" 略有夸大** | 🟡 MEDIUM | 源代码中仅有 `inventory_api_server.py` 的 3 个 API 端点（L76/109/136）有 `if not product_id` 的基本校验，`routes_core.py` 的 `stock_in`/`stock_out` 只有 `data.get('qty', 0)` 没有类型校验。实际有校验的路由约 3-4 个，不足 6 个。 |

#### TASK 文档数字不一致（−2 分）

| # | 问题 | 说明 |
|---|------|------|
| F-04 | **TASK 文档声称"14个原子任务"但实际列出 16 个** | TASK 文档概要文字写"本任务将库存系统安全加固与代码修复拆分为 14 个原子任务"，但实际任务清单列出 P0(5)+P1(7)+P2(2)+P3(2)=**16 个**。数字不一致会影响排期计算。 |

---

### 2.2 覆盖完整性 — 17/20 ✅

#### 已覆盖（+17 分）

| 问题编号 | 是否覆盖 | 备注 |
|----------|----------|------|
| S-01 SQL注入(routes_data.py) | ✅ 覆盖 | 虽事实有误，但功能增强方向正确 |
| S-02 SQL注入(routes_api.py) | ✅ 覆盖 | 正确识别列名拼接问题 |
| S-03 路径遍历 | ✅ 覆盖 | 修复方案完整（secure_filename + realpath） |
| S-04 命令注入 | ✅ 覆盖 | 修复方案完整（正则校验 + 异常处理） |
| S-05 事务回滚 | ✅ 覆盖 | try-except-rollback 模式完整 |
| S-06 并发控制 | ✅ 覆盖 | web 端 FOR UPDATE，区分 API 端已有锁 |
| S-07 错误脱敏 | ✅ 覆盖 | 完整清单，含 logger.exception 替换 |
| S-08 输入验证 | ✅ 覆盖 | validate_required 函数设计合理 |
| S-09 权限控制 | ✅ 覆盖 | check_auth 白名单调整方案清晰 |
| S-10(新) 主入口安全 | ✅ 覆盖 | 4 类问题全部识别 |
| S-11 硬编码清理 | ✅ 覆盖 | safety_stock=0, max_stock=999999 |
| S-12 数据约束 | ✅ 覆盖 | 完整性约束 |
| S-13 审计日志 | ✅ 覆盖 | 写操作埋点，非重头构建 |
| S-14(新) XSS防护 | ✅ 覆盖 | CSP + 安全头 + 模板审计 |
| S-15 服务层 | ✅ 覆盖 | P3 持续演进，定位合理 |
| S-16 性能优化 | ✅ 覆盖 | P3 持续，定位合理 |

#### 未覆盖（−3 分）

| # | 遗漏项 | 严重性 | 说明 |
|---|--------|--------|------|
| M-01 | **搁置事项`save_settings`明文密码写文件无应对** | 🟠 HIGH | `routes_api.py:54-63` 的 `save_settings` 函数将数据库密码明文写入 `inventory_config.json`，虽然是搁置事项但与 S-03 路径遍历关联声明不够充分。S-03 修复后密码文件仍可被服务器上其他进程读取。应在方案中补充风险说明或给出临时缓解措施（如修改文件权限为 600）。 |
| M-02 | **products.html 内联 onclick 含未转义模板变量** | 🟡 MEDIUM | `products.html:25` 中 `onclick="editRow('product',{{ p.id }},{name:'{{ p.name }}',...})"`，`p.name` 和 `p.spec` 来自数据库，虽非直接用户输入但存在存储型 XSS 风险（如名称被写入恶意数据时）。TASK-010（XSS防护）未涵盖此类内联事件处理器中的模板变量风险。 |
| M-03 | **缺少 session cookie 安全配置** | 🟡 MEDIUM | `inventory_api_server.py` 未设置 `SESSION_COOKIE_SECURE`、`SESSION_COOKIE_HTTPONLY`、`SESSION_COOKIE_SAMESITE`。TASK-005 的子任务 5.4（安全响应头）未包含 session cookie 相关设置。 |

---

### 2.3 依赖关系合理性 — 11/15 ✅

#### 合理的依赖（+8 分）

| 依赖 | 合理性 | 说明 |
|------|--------|------|
| T001 → T006 | ✅ 合理 | SQL注入修复完再做输入验证层 |
| T002 → T006 | ✅ 合理 | 同上 |
| T006 → T007 | ✅ 合理 | 先验证再写入，再回滚 |
| T006 → T008 | ✅ 合理 | 先验证再并发控制 |
| T007 → T012 | ✅ 合理 | 事务稳定后再加审计 |
| T008 → T012 | ✅ 合理 | 并发控制稳定后再加审计 |
| T005 → T009 | ✅ 合理 | 主入口异常先修，再全量脱敏 |
| T005 → T010 | ✅ 合理 | 主入口登录页模板化后，再扩展全局XSS防护 |

#### 不合理的依赖（−4 分）

| # | 依赖 | 问题 | 建议 |
|---|------|------|------|
| D-01 | **S03 → S04 (依赖图中)** | 路径遍历和命令注入是 `routes_system.py` 中两个完全独立的函数（`backup_restore` 和 `backup_create`），无数据依赖。 | 改为并行关系 |
| D-02 | **T011 → T015** | API路由权限控制（T011）是安全加固，服务层（T015）是架构优化。权限控制可以独立完成，服务层依赖权限控制不合理。 | 改为弱依赖或删除箭头 |
| D-03 | **T015 → T016** | 性能优化（T016）依赖服务层（T015）的引入，但部分性能优化（如加索引、连接池优化）无需服务层即可独立执行。 | 改为弱依赖：部分性能项可并行 |

---

### 2.4 代码质量 — 13/15 ✅

#### 优秀实践（+10 分）

| 实践 | 位置 | 说明 |
|------|------|------|
| `secure_filename` + `realpath` 双层校验 | TASK-003 | 防御深度设计，路径遍历防护完整 |
| `validate_required` 兼容字符串数字 | TASK-006 | `try: int(val)` 替代 `isinstance`，解决前次评测的 B-4 缺陷 |
| `logger.exception()` 替代 `str(e)` | 所有任务 | 完整堆栈记录，不泄露细节 |
| `FOR UPDATE` + 锁等待超时 | TASK-008 | 悲观锁+超时保护，预防死锁 |
| `raise RuntimeError` 硬性拒绝无密码启动 | TASK-005 | 安全先行，不静默降级 |

#### 可改进项（−2 分）

| # | 问题 | 说明 |
|---|------|------|
| C-01 | **`ALLOWED_FIELDS` 白名单放在路由函数外** | TASK-001 的 `ALLOWED_FIELDS` 建议定义在模块顶部，但 `routes_data.py` 已有类似模式（白名单字典），建议统一放在模块级别便于维护和测试。现有方案中 TASK-001 和 TASK-002 分别定义了不同的白名单（`ALLOWED_FIELDS` vs `ALLOWED_UPDATE_FIELDS`），命名不一致。 |
| C-02 | **`check_auth` 改造方案缺少单元测试指引** | TASK-011 的 `check_auth` 改造会改变 `/api/` 路由的访问行为，但未提供测试方法或回退方案。建议增加手动测试步骤或临时回退开关（如环境变量 `API_OPEN_ACCESS=1`）。 |

---

### 2.5 可执行性 — 13/15 ✅

#### 优点（+10 分）

| 优点 | 说明 |
|------|------|
| 原子化拆分清晰 | 每个任务独立文件，独立验收标准 |
| 估算工时合理 | P0 3天、P1 5天、P2 2天，总计约 10 天即可完成核心加固 |
| 并行执行优化 | P0 的 5 个任务无依赖关系，可并行 |
| 验收标准 checkbox 化 | 每个任务都有明确的可勾选验收项 |
| 行为变更说明 | TASK-001 明确标注了"行为变更说明"，利于评估影响 |

#### 可改进项（−2 分）

| # | 问题 | 说明 |
|---|------|------|
| E-01 | **`save_settings` 回滚方案缺失** | 安全加固后 `save_settings` 功能将因 `check_auth` 改造（TASK-011）而被拦截（`/api/` 不再放行），但方案未说明该接口如何处理（添加 cookie-based 认证 vs 移除该接口 vs 保留白名单例外）。 |
| E-02 | **TASK-006 修改 4 个文件，依赖复杂** | TASK-006 需同时修改 `db_utils.py`、`routes_core.py`、`routes_data.py`、`routes_api.py`，横跨 4 个文件，建议拆为 2 个子任务：6a) `db_utils.py` 添加 `validate_required`；6b) 各路由添加校验调用。 |

---

### 2.6 文档一致性 — 7/10 ⚠️

| # | 问题 | 说明 |
|---|------|------|
| I-01 | **任务计数不一致** | 概要写"14个原子任务"，清单列出 16 个 |
| I-02 | **修订版方案与TASK文档的依赖图结构不同** | 修订版方案 mermaid 图使用 `S01`/`S02` 编号，TASK 文档使用 `T001`/`T002` 编号，但两者箭头连接略有差异（修订版有 `S01 --> S08` 和 `S02 --> S08`，TASK 文档完全一致）。总体一致但编号不统一可能造成混淆。 |
| I-03 | **序号跳跃** | 问题清单从 S-10 跳到 S-14（原 S-11/S-12/S-13 保留编号但内容已被替换），新编号读者需要对照表格理解，建议直接使用连续编号降低认知负荷。 |

---

## 三、核心问题汇总（按修复优先级）

### P0 — 必须修复后方可执行

| 编号 | 问题 | 所属任务 | 修复要求 |
|------|------|----------|----------|
| **F-01** | TASK-001 将无漏洞代码标注为 P0 CRITICAL | TASK-001 | 重写 TASK-001 任务描述：从"SQL注入漏洞修复"改为"base_add 添加动态字段白名单支持（功能增强）"，优先级从 P0 降至 **P2**。或保留 P0 但标注为"安全加强"而非"漏洞修复"。 |
| **F-02** | 依赖图 S03→S04 错误 | 修订版方案 | 删除 S03→S04 箭头，改为并行关系 |

### P1 — 建议修复

| 编号 | 问题 | 所属任务 | 修复要求 |
|------|------|----------|----------|
| **F-04** | 任务计数 14 vs 16 不一致 | TASK 文档 | 修改概要文字为"16个原子任务"，或调整实际任务数量 |
| **M-01** | `save_settings` 明文密码无应对 | 修订版方案 | 在搁置事项中补充补充风险说明：S-03 修复后密码文件仍可被同进程读取，建议修改文件权限为 600 或加密存储 |
| **M-02** | 模板内联 `onclick` 含未转义变量 | TASK-010 | 补充对 `products.html:25` 类似模式的风险说明 |
| **M-03** | Session cookie 安全配置缺失 | TASK-005 | 在子任务 5.4 补充：`app.config['SESSION_COOKIE_HTTPONLY'] = True` |
| **E-01** | `save_settings` 回滚方案缺失 | TASK-011 | 补充说明 `save_settings` 接口在权限控制改造后的处理方案 |
| **C-01** | 白名单命名不统一 | TASK-001/002 | 统一为 `ALLOWED_FIELDS_xxx` 或 `ALLOWED_COLUMNS_xxx` 模式 |

---

## 四、与初版方案的对比

| 维度 | 初版 | 修订版(v2.0) | 改进 |
|------|------|-------------|------|
| 事实错误 | 8 项 | **2 项**（F-01 事实夸大 + F-02 依赖错误） | ↑ 75% 改进 |
| 代码缺陷 | 4 项 | **1 项**（C-01 命名不统一） | ↑ 75% 改进 |
| 遗漏问题 | 5 项 | **3 项**（M-01/M-02/M-03） | ↑ 40% 改进 |
| 依赖关系错误 | 3 项 | **1 项**（S03→S04） | ↑ 67% 改进 |
| 综合评分 | ~45/100 (D) | **79/100 (B)** | ↑ 34 分 |

> 从 D 级跳升至 B 级，核心改进源于删除了两个虚假问题（连接池、蓝图）、修正了优先级和依赖关系、新增了 inventory_api_server.py 和 XSS 防护任务。

---

## 五、最终结论

### 总评：79/100 — ⚠️ B 级（良好，建议修正后执行）

**优点**：
- 相比初版改进显著（从 45 分提升至 79 分）
- 任务拆分原子化程度高，验收标准明确
- `inventory_api_server.py` 和 XSS 防护的补充使覆盖更完整
- `validate_required` 设计和异常处理模式质量良好

**主要风险**：
1. **TASK-001 事实夸大**：将无漏洞代码标为 P0 CRITICAL，如不修正将导致执行资源浪费（建议降级为 P2 功能增强）
2. **TASK-006 横跨 4 文件**：复杂度较高，建议拆分子任务
3. **`save_settings` 和 session cookie 未处理**：安全加固存在缺口

### 修正后执行建议

| 步骤 | 操作 | 预计耗时 |
|------|------|----------|
| 1 | 修正 F-01：TASK-001 改为 P2 功能增强 | 10 分钟 |
| 2 | 修正 F-02：删除 S03→S04 依赖箭头 | 5 分钟 |
| 3 | 修正 F-04：统一任务计数为 16 | 5 分钟 |
| 4 | 补充 M-01/M-02/M-03 的应对说明 | 15 分钟 |
| 5 | 补充 E-01 `save_settings` 处理方案 | 10 分钟 |
| 6 | 统一 C-01 白名单命名 | 5 分钟 |
| — | **总修正耗时** | **约 50 分钟** |
| — | **修正后可执行评级** | **→ 92/100 (A-)** |
