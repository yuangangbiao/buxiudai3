# 硬编码检查报告 - 2026-05-09

## 1. 执行摘要
针对 `wechat_work_bot_v2.py` 及其相关模块进行硬编码专项审查，共发现 **6 处硬编码问题**。
整体评级：**需要修复**

## 2. 硬编码问题清单

### 2.1 操作员映射表硬编码 [MEDIUM]
- **文件**: [wechat_work_bot_v2.py](file:///d:/yuan/不锈钢网带跟单3.0/mobile_api_ai/wechat_work_bot_v2.py#L67-L72)
- **代码**:
```python
_WECHAT_NAME_MAP = {
    'OP001': {'name': '张三', 'wechat': '@张三'},
    'OP002': {'name': '李四', 'wechat': '@李四'},
    'OP003': {'name': '王五', 'wechat': '@王五'},
    'OP004': {'name': '赵六', 'wechat': '@赵六'},
    'MG001': {'name': '钱经理', 'wechat': '@钱经理'},
}
```
- **说明**: 操作员名称和微信 ID 直接硬编码在代码中，无法动态配置。新增操作员需要改代码
- **建议**: 读取 `.env` 中的 `OPERATOR_MAP` JSON 配置，或从数据库动态获取

### 2.2 数据库文件名硬编码 [MEDIUM]
- **文件**: [wechat_work_bot_v2.py](file:///d:/yuan/不锈钢网带跟单3.0/mobile_api_ai/wechat_work_bot_v2.py#L244)
- **代码**: `_db_path = os.path.join(_project_root, 'wechat_container.db')`
- **说明**: 数据库文件名 `wechat_container.db` 硬编码，应该从 `.env` 中读取（如 `CONTAINER_DB_PATH`）

### 2.3 默认数据库文件名硬编码 [MEDIUM]
- **文件**: [storage_layer.py](file:///d:/yuan/不锈钢网带跟单3.0/mobile_api_ai/storage_layer.py#L203)
- **代码**: `def __init__(self, db_path: str = 'container_storage.db'):`
- **说明**: `SQLiteStorage` 的默认路径为 `container_storage.db`，应该从配置获取

### 2.4 调度间隔硬编码 [LOW]
- **文件**: [wechat_work_bot_v2.py](file:///d:/yuan/不锈钢网带跟单3.0/mobile_api_ai/wechat_work_bot_v2.py#L2114)
- **代码**: `start_background_scheduler(interval_seconds=60)`
- **说明**: 后台调度器间隔固定为60秒，应改为从 `.env` 中读取（如 `SCHEDULER_INTERVAL=60`）

### 2.5 日志URL硬编码 [LOW]
- **文件**: [wechat_work_bot_v2.py](file:///d:/yuan/不锈钢网带跟单3.0/mobile_api_ai/wechat_work_bot_v2.py#L2062)
- **代码**: `logger.info('  [WEB] 网页首页：http://localhost:5003')`
- **说明**: 启动日志中的网址硬编码为 `localhost:5003`，应该使用 `config.Config.WECHAT_BOT_HOST` 和 `config.Config.WECHAT_BOT_PORT`

### 2.6 调度器默认间隔硬编码 [LOW]
- **文件**: [dispatch_center.py](file:///d:/yuan/不锈钢网带跟单3.0/mobile_api_ai/dispatch_center.py#L1866)
- **代码**: `def start_background_scheduler(interval_seconds: int = 60):`
- **说明**: 函数参数默认值60秒硬编码，应使用配置

## 3. 已确认正常（无问题）项

| 检查项 | 结果 |
|--------|------|
| API密钥硬编码 | ✅ 全部使用 `os.getenv()` 从环境变量读取 |
| 密码硬编码 | ✅ 全部使用 `os.getenv()` 从环境变量读取 |
| 颜色硬编码（ANSI） | ✅ 未发现硬编码颜色 |
| print调试语句 | ✅ 无（只有build脚本中有print，可接受） |
| 裸except语句 | ✅ 全部使用 `except Exception as e:` |
| 服务器地址/端口 | ✅ 从 `.env` 读取 `WECHAT_BOT_HOST` / `WECHAT_BOT_PORT` |

## 4. 修复优先级

| 优先级 | 问题 | 影响 |
|--------|------|------|
| P1 | - | 无紧急硬编码安全问题 |
| P2 | 操作员映射表(L67-72)、数据库路径(L244) | 影响部署和扩展 |
| P3 | 调度间隔(L2114)、日志URL(L2062) | 运维友好性 |

## 5. 修复建议

操作员映射表可通过 `.env` 配置：
```ini
# .env 新增
OPERATOR_MAP={"OP001": {"name": "张三", "wechat": "@张三"}, "OP002": {"name": "李四", "wechat": "@李四"}}
```

数据库路径通过 `.env` 配置：
```ini
# .env 新增
CONTAINER_DB_PATH=wechat_container.db
```
